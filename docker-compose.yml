services:
  tallee:
    image: ghcr.io/frikyfriky11/tallee:main
    # build: .   # uncomment to build locally instead
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      # Optional: set these to enable Umami analytics
      UMAMI_WEBSITE_ID: ""
      UMAMI_URL: ""
